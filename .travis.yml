language: php
php:
    - 5.5
    - 5.6

install:
  - mkdir app/temp && chmod 777 app/temp
  - mkdir app/log && chmod 777 app/log

before_script:
  - composer self-update
  - composer install --no-interaction --prefer-source --dev

script: "vendor/bin/codecept run"
